ZED camera
==========
We use ZED camera as our visual input.

Install cv-bridge
-----------------

Install cv-brdige package through apt-get.

.. code-block:: bash

    sudo apt update
    sudo apt install -y ros-melodic-cv-bridge
    sudo apt install -y ros-melodic-vision-opencv

